<!DOCTYPE html>
<html>

<%- include('header', {css: 'set-filter'}); %>
<div class="filter-container row fill-parent">
  <form class="row filter-form">
    <div
      class="playlist-card mdl-card selected dont-shrink mdl"
      style="--playlist-img: url(<%- state.statePlaylist.image || `${cdn}assets/empty-playlist.jpg` %>)"
    >
      <div class="mdl-card__title mdl-card--expand">
      </div>

      <div class="mdl-card__actions">
        <div class="playlist-card-name">
          <%=state.statePlaylist.name %>
        </div>
      </div>
    </div>

    <div class="set-filter-card mdl-card mdl-card--expand dont-shrink">
      <div class="mdl-card__title">
        <h2 class="mdl-card__title-text">Set filters</h2>
      </div>


      <div class="mdl-card__supporting-text mdl-card--expand">
        <div style="padding-bottom: 0.5rem;">Select how to filter your playlist. For example, if you type 5 in the
          Artists box,
          the playlist will only keep songs by artists you listened to less than 5 times.</div>
        <form action="#">

          <div
            id="name"
            class="grow"
          >
            <div class="mdl-textfield mdl-js-textfield mdl-textfield--floating-label fill-parent">
              <input
                class="mdl-textfield__input"
                type="number"
                min=1
                id="song-input"
              >
              <label
                class="mdl-textfield__label"
                for="song-input"
              >Threshold to filter songs by</label>
            </div>
            <div class="mdl-textfield mdl-js-textfield mdl-textfield--floating-label fill-parent">
              <input
                class="mdl-textfield__input"
                type="number"
                min=1
                id="artist-input"
              >
              <label
                class="mdl-textfield__label"
                for="artist-input"
              >Threshold to filter artists by</label>
            </div>
          </div>
          <div class="row filter-form-actions">
            <button
              type="button"
              id="filter-btn"
              class="mdl-button mdl-js-button mdl-button--raised mdl-js-ripple-effect mdl-button--accent"
            >
              Filter
            </button>
          </div>
        </form>
      </div>

    </div>
  </form>


</div>
<%- include('footer', {}) %>